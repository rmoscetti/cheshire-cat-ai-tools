# the cat doesn't allow for editable/local dependencies due to the reliance on https://packaging.pypa.io/en/stable/requirements.html
# this script will install the dependencies in the editable form
pip install -e /drymulator-client